datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Notification {
  id      String   @id @default(uuid())
  to      String
  subject String
  body    String
  sentAt  DateTime @default(now())
}
